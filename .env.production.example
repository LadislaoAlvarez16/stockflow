# Base de datos
POSTGRES_USER=admin
# openssl rand -hex 24
POSTGRES_PASSWORD=
POSTGRES_DB=stockflow

# Redis
# openssl rand -hex 24
REDIS_PASSWORD=

# JWT
# openssl rand -hex 48
JWT_SECRET=
JWT_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d

# Webhooks
# openssl rand -hex 32
WEBHOOK_ENCRYPTION_KEY=

# App
FRONTEND_URL=https://app.midominio.com

# Bull Board (Panel de colas)
BULL_BOARD_USER=admin
BULL_BOARD_PASSWORD=

# Admin Inicial (sólo para la primera ejecución)
INITIAL_ADMIN_EMAIL=admin@midominio.com
INITIAL_ADMIN_PASSWORD=

# SMTP (Opcional)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
MAIL_FROM=

# API Port
API_PORT=3000
